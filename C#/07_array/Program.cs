using System;

namespace _07_array
{
    class Program
    {
        static void Main(string[] args)
        {
            // 声明数组
            int[] arr = new int[10]; // 创建一个长度为10的int类型数组，每个元素初值为0
            string[] str = new string[10]; // 创建一个长度为10的string数组，每个元素初值为null。null表示未开辟空间，""表示已开辟空间，但没有值
            bool[] bools = new bool[10]; // // 创建一个长度为10的bool数组，每个元素初值为false
            // 声明并赋值数组
            int[] arr1 = { 1, 2, 3 };
        }
    }
}
